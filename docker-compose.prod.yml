version: '3.8'

# Production Docker Compose Configuration
# Use: docker-compose -f docker-compose.prod.yml up
# Note: This is a reference - adjust based on your production needs

services:
  # Backend service (production)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: spotify-downloader-backend-prod
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=production
      - PORT=8000
    volumes:
      # Only mount temp/uploads folders, not entire codebase
      - ./backend/controllers/temp:/app/controllers/temp
      - ./uploads:/app/uploads
    networks:
      - spotify-network
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

  # Frontend service (production - nginx)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    container_name: spotify-downloader-frontend-prod
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - spotify-network
    restart: always
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 128M

networks:
  spotify-network:
    driver: bridge
